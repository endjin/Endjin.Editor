namespace Endjin.Editor.Model {
    /**
     * Specifies the direction for text rendering
     * */
    export enum TextDirection {
        /* automatic or inherited */
        Auto = "auto",
        /* left to right */
        LeftToRight = "ltr",
        /* right to left */
        RightToLeft = "rtl"
    }
}